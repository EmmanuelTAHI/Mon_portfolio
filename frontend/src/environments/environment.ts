export const environment = {
  production: false,
  apiBase: '/api',
  /** Base URL du backend pour les médias (images). Si l'API renvoie une URL relative, on la préfixe avec celle-ci. */
  backendMediaBase: 'http://localhost:8000',
  /** EmailJS : formulaire de contact (envoi depuis le navigateur). */
  emailJsPublicKey: 'vREBuWY3ZCsTyIFKc',
  emailJsServiceId: 'service_dvdfxfg',
  emailJsTemplateId: 'template_8gnwokd',
};
