# Fixtures pour la production (Render)

Place ici le fichier **portfolio_data.json** exporté depuis ta base locale, puis exécute sur Render (Shell) :

```bash
python manage.py load_production_data
```

Voir **SYNC_DATA.md** à la racine du repo pour les étapes complètes (export local → import Render).
